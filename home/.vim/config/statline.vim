" Config settings for the vim-statline plugin.

" Enable all the integrations.
" let g:statline_syntastic = 1
" let g:statline_fugitive = 1
" let g:statline_rvm = 1

" Display "NOE" for unencoded files rather than "No Encoding" to help with
" verbosity.
" let g:statline_no_encoding_string = 'NOE'
