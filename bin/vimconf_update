#!/bin/bash
SCRIPT_PATH = `which $0`
REPO_DIR = ${SCRIPT_PATH%/*/*}

# change into the repository root
cd $REPO_DIR

# update any git submodules
git submodule update

# update using GLVS
vim +GLVS +q

exit
