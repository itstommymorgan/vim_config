Welcome to my vim config!

Feel free to poke around.

Install:
    git clone git@github.com:duwanis/vim_config.git
    cd ~
    ln -s /path/to/vim_config/vim .vim
    ln -s /path/to/vim_config/vimrc .vimrc

Done.
